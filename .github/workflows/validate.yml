name: validate
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  invariants:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck

      - name: Shellcheck all hooks and scripts
        run: |
          # Severity 'warning' is the industry-standard CI gate.
          # 'style' and 'info' levels are for local dev preference, not CI hard-fail.
          for f in hooks/*.sh scripts/*.sh; do
            echo "--- shellcheck $f"
            shellcheck --severity=warning "$f" || exit 1
          done

      - name: Run protocol invariants
        run: bash scripts/validate.sh

      - name: gitleaks secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: false

